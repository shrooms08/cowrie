{
  "phase": "R4 — UI cleanup, claimable names, no hardcoded merchant",
  "date": "2026-07-01",
  "no_contract_changes": true,
  "fallback_commit": "73c7097 (R3; R4 changes are uncommitted working tree)",
  "cleanups": {
    "identity": "wallet card reads '<name>.cowrie' (removed 'account')",
    "helper_line_removed": "the 'real testnet USDC in your account…' line is gone",
    "private_label": "reads 'COWRIE PRIVATE BALANCE' (removed '· in the pool')",
    "honesty_block": "removed from wallet UI; retained in README honesty ledger"
  },
  "claimable_names": {
    "buyer": "'claim name' -> type -> '<slug>.cowrie', persists across reload (e2e: mychosen.cowrie)",
    "merchant": "sign-in form starts empty; custom business name (e2e: 'Acme Foods'), persists; validated 2-40 chars",
    "note": "names are a LOCAL claim (no on-chain registry) — documented in README"
  },
  "no_hardcoded_merchant": "grep app/ lib/ for 'Buka Express'/'Test Merchant' -> NONE; createMerchant has no default name; buyer manual-pay fallback uses getMerchant() (null-safe, no auto-create)",
  "e2e": "R4 CLEANUP + CLAIMABLE NAMES ✓ — clean labels, buyer claim persists, merchant 'Acme Foods' persists, full loop (deposit $50, pay $25, merchant real USDC Δ +25.00, register flipped), receipt ok, can't-cover graceful",
  "readme_honesty_ledger": "intact + added 'names are a local claim' note and 'on-chain name registry' to production-needs",
  "build": "next build green"
}
