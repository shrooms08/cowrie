[package]
name = "contract-types"
version = "0.0.0"
edition = "2021"
license = "MIT"
publish = false

[lib]
crate-type = ["lib"]
doctest = false

[dependencies]
# external
soroban-sdk = { version = "26", features = ["alloc"] }

